# Prototype latency report

Date:

Commit or source snapshot:

Device and operating system:

Network and provider region:

STT, LLM, TTS, VAD, and turn models:

Configuration:

## Results

- Sample count:
- End-of-turn to first final transcript p50 / p95 / max:
- End-of-turn to LLM first token p50 / p95 / max:
- End-of-turn to first TTS audio p50 / p95 / max:
- End-of-turn to browser playout p50 / p95 / max:
- Verified barge-in to silent playout p50 / p95 / max:
- Stale frames accepted after interruption:
- Provider failure recovery rate:

## Accuracy

- Complete-order accuracy:
- Item accuracy:
- Modifier accuracy:
- Premature end-of-turn rate:
- False barge-in rate:

## Interpretation

Record which target passed or failed, the largest observed bottleneck, and the
next experiment. Keep raw consented audio outside version control and link only
to the controlled test dataset reference.
